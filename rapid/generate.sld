(define-library (rapid generate)
  (import (scheme base) (rapid module) (rapid assemble) (rapid asmjs))
  (begin
  
    
    
    


    ; TEST
   (emit-js
    (assemble-module
     ;(error (error 
        (module 5
          `((
            (set! ,(local 2 3) ,(string-const "What's up?"))
          
            
            
            )))))
    (newline)
 
 
 
            
            
  
  
  ))
