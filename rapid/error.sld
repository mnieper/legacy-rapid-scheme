(define-library (rapid error)
  (export compile-error runtime-error)
  (import (scheme base))
  (begin
  
    ; FIXME Define and implement compile-error and runtime-error.
    ; TODO Use this library.
  
  ))
