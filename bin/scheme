#!/bin/sh

SCHEME=chibi-scheme
BASEDIR=$(dirname $0)/..
COMPILE=$BASEDIR/bin/rapid-scheme
OUTPUT=$(dirname "$1")/$(basename "$1" .scm).bin

$COMPILE $1 > $OUTPUT

shift

$SCHEME -I$BASEDIR/lib -I$BASEDIR/rapid-lib -I$BASEDIR/rapid-macros -I$BASEDIR/rapid-read -- $OUTPUT $*
